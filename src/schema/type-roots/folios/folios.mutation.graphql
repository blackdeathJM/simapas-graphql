type Mutation
{
    "FOLIOS - Registro de folio con el folio auto generado"
    registroFolio(folio: FolioInput!): FolioResType!
    "FOLIOS - Actualizar archivoUrl"
    acUrlFolio(id: ID!, archivoUrl: String!): FolioType!
}
