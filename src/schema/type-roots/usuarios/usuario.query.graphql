extend type Query
{
    "USUARIO - Obtener la lista de todos los usuarios"
    obtenerUsuarios: [UsuarioType]
    "USUARIO - Inicio de sesion"
    login(usuario: String!,  contrasena: String!): UsuarioTokenType!
    "USUARIO - Validar token de usuario"
    validarTokenG: UsuarioResType!
    "USUARIO - Buscar usuario por su nombre de usuario"
    buscarUsuario(usuario: String!): UsuarioResType!
}
