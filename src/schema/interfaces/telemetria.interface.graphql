interface IInstalacion
{
    _id: ID
    nombre: String
    direccion: String
    noInstalacion: Int
    activo: Boolean
}

interface ITelemetria
{
    radio: [String]
    plc: [String]
    switch: [String]
    repetidor: [String]
}

interface IBomba
{
    descripcion: String
    marca: String
    serie: String
    hp: String
    fechaInstalacion: String
    FechaRetiro: String
    activa: Boolean
}

interface IArrancador
{
    descripcion: String
}
