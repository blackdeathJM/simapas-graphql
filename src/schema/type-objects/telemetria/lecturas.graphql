type LecturasTType
{
    macro: [LecturasType]
    nivelD: [LecturasType]
    nivelE: [LecturasType]
    cfe: [LecturasType]
}

type LecturasType implements ILecturas
{
    id: ID
    ano: Int
    mes: Int
    dia: Int
    lectura: Float
}
