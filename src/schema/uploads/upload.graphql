scalar Upload

type FileType
{
    filename: String
    mimetype: String
    encoding: String
}

type Mutation
{
    subidaSimple(file: Upload!): FileType
    subidaMultiple(files: [Upload!]!): FileType
}
